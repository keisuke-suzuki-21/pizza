<% @page_title = "カート内情報" %>
<h1><%= @page_title %></h1>

<% if @order.present? %>
  <% @order.products.each do |product| %>
    <h5><%= product.mainmenu.name %></h5>
  <% end %>
<% else %>
 <p>カートに情報がありません。</p>
<% end %>

<div><%= link_to "<<商品一覧へ", mainmenus_path %></div>
