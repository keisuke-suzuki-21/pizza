<% @page_title = "カート内情報" %>
<h1><%= @page_title %></h1>

<% if @order.products.present? %>
  <% @order.products.each do |product| %>
    <h5><%= product.mainmenu.name %></h5>
  <% end %>
<% else %>
 <p>カートに情報がありません。</p>
<% end %>

<div><%= link_to "注文ボタン", edit_order_path(@order) %></div>
